# ==================== SERVER ====================
NODE_ENV=development
PORT=8888

# ==================== DATABASE ====================
# Local example:
# MONGODB_URI=mongodb://localhost:27017/bassac_media_center
# MongoDB Atlas example (recommended for online hosting):
# MONGODB_URI=mongodb+srv://<db_user>:<db_password>@<cluster-url>/bassac_media_center?retryWrites=true&w=majority&appName=<cluster-name>
MONGODB_URI=mongodb://localhost:27017/bassac-media

# ==================== JWT ====================
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_REFRESH_SECRET=your-super-secret-refresh-key-change-this-in-production
JWT_EXPIRES_IN=1d
JWT_REFRESH_EXPIRES_IN=7d

# ==================== REDIS (OPTIONAL) ====================
# Leave commented out if you don't have Redis installed
# The app will work fine without Redis (just no caching)
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_PASSWORD=

# ==================== EMAIL ====================
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
EMAIL_FROM=noreply@bassac.media

# ==================== FRONTEND ====================
FRONTEND_URL=http://localhost:5173
# Public website URL used in Open Graph tags and share pages
SITE_URL=http://localhost:5173
CORS_ORIGIN=http://localhost:5173
# CSRF cookie settings for cross-domain frontend/backend hosting
# Use "none" on platforms like Render when frontend and backend are on different domains.
# When sameSite=none, secure must be true.
CSRF_COOKIE_SAMESITE=lax
CSRF_COOKIE_SECURE=false

# ==================== FILE UPLOAD ====================
MAX_FILE_SIZE=10485760
UPLOAD_PATH=./uploads
# Storage provider: local | cloudinary
STORAGE_PROVIDER=local
# If true, failed Cloudinary uploads will fall back to local storage
STORAGE_FALLBACK_TO_LOCAL=true

# ==================== OPTIONAL SERVICES ====================
# Uncomment only if you need these features

# Cloudinary (cloud storage)
# CLOUDINARY_CLOUD_NAME=your-cloud-name
# CLOUDINARY_API_KEY=your-api-key
# CLOUDINARY_API_SECRET=your-api-secret
# OR single URL form:
# CLOUDINARY_URL=cloudinary://<api_key>:<api_secret>@<cloud_name>

# Elasticsearch (advanced search)
# ELASTICSEARCH_NODE=http://localhost:9200
# ELASTICSEARCH_USERNAME=
# ELASTICSEARCH_PASSWORD=

# OpenAI (AI assistant)
# OPENAI_API_KEY=your-openai-api-key

# Google OAuth (social login)
# GOOGLE_CLIENT_ID=your-google-client-id
# GOOGLE_CLIENT_SECRET=your-google-client-secret
# GOOGLE_REDIRECT_URI=http://localhost:8888/api/auth/social/google/callback

# Facebook OAuth (social login)
# FACEBOOK_APP_ID=your-facebook-app-id
# FACEBOOK_APP_SECRET=your-facebook-app-secret
# FACEBOOK_REDIRECT_URI=http://localhost:8888/api/auth/social/facebook/callback

# Frontend callback URL after social auth
# SOCIAL_AUTH_CALLBACK_URL=http://localhost:5173/auth/social/callback

# ==================== SESSION ====================
SESSION_SECRET=your-session-secret-change-this-in-production

# ==================== RATE LIMITING ====================
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX=100
